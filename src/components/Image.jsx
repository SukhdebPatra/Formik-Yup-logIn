import React from "react";

const Image = () => {
  return (
    <div
      className="Rectangle5"
      style={{
        width: 402,
        height: 211,
        background:
          "linear-gradient(0deg, rgba(0, 0, 0, 0.80) 0%, rgba(0, 0, 0, 0) 100%)",
      }}
    ></div>
  );
};

export default Image;
